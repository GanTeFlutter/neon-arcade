import 'package:akillisletme/product/generated/fonts.gen.dart';

/// Merkezi font sabitleri.
/// Hardcoded string yerine bu sabitleri kullanın.
class AppFonts {
  AppFonts._();

  /// Futuristic başlık/heading fontu (Orbitron).
  static const neonDisplay = FontFamily.orbitron;

  /// Retro skor/label fontu (PressStart2P).
  static const neonScore = FontFamily.pressStart2P;

  /// Okunabilir body/title fontu (Inter).
  static const body = FontFamily.inter;

  /// Display/heading fontu (Poppins).
  static const display = FontFamily.poppins;
}
